# Поиск закупок на zakupki.gov.ru

Инструмент для автоматизированного поиска закупок на портале [zakupki.gov.ru](https://zakupki.gov.ru) с выгрузкой результатов в Excel и опциональной отправкой по e-mail.

---

## Цель

Автоматически собирать данные о закупках из двух источников:

- **docSearch** — `https://zakupki.gov.ru/epz/order/docSearch/results.html`
- **extendedsearch** — `https://zakupki.gov.ru/epz/order/extendedsearch/results.html`

применять фильтр по региону через модальное окно «Мой регион» («Ваш субъект РФ» → Сохранить), объединять результаты, экспортировать в XLSX и при необходимости отправлять по e-mail.

---

## 🐳 Запуск через Docker (рекомендуется)

Это самый простой способ: не нужно устанавливать Python, Playwright или браузер вручную — всё включено в образ.

### Предварительные требования

- [Docker Desktop](https://www.docker.com/products/docker-desktop/) для Windows / macOS  
  (или Docker Engine для Linux)

### Шаги

```bash
# 1. Клонировать репозиторий
git clone https://github.com/rpro15/Search_purchases-.git
cd Search_purchases-

# 2. Собрать образ и запустить контейнер
docker compose up --build
```

После успешного старта откройте браузер по адресу: **http://localhost:8501**

### Где находятся файлы экспорта

Файлы `results.xlsx` сохраняются в папку `output/` в корне репозитория (она монтируется в контейнер).

### Остановка

```bash
docker compose down
```

---

## 💻 Локальный запуск (Windows / macOS / Linux)

### 1. Клонировать репозиторий

```bash
git clone https://github.com/rpro15/Search_purchases-.git
cd Search_purchases-
```

### 2. Создать виртуальное окружение и установить зависимости

**Windows:**

```bat
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

**macOS / Linux:**

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 3. Установить браузер Playwright

```bash
playwright install chromium
```

### 4. Запустить приложение

```bash
streamlit run app.py
```

Браузер откроется автоматически по адресу `http://localhost:8501`.

---

## Использование UI

1. В боковой панели введите **поисковый запрос** и **регион** (по умолчанию `г Москва`).
2. Настройте диапазон дат, переключатели источников и лимит результатов.
3. При необходимости включите **AI-ранжирование** и задайте порог.
4. Для отправки по e-mail укажите адрес получателя и выберите режим:
   - **📬 Открыть почтовый клиент (без пароля)** — создаёт ссылку `mailto:`, которая открывает ваш почтовый клиент. Файл Excel нужно прикрепить вручную.
   - **📤 SMTP (mail.ru, с паролем)** — автоматически отправляет письмо с вложением через mail.ru SMTP. Требует логин и пароль приложения.
5. Нажмите **▶ Запустить поиск**.
6. Просмотрите таблицу результатов и скачайте Excel-файл.

---

## 🛠 Устранение неполадок

### Playwright: `NotImplementedError` на Windows

На некоторых версиях Windows/Python возникает ошибка:

```
NotImplementedError: There is no current event loop in thread 'MainThread'.
```

**Решение:** убедитесь, что используете Python 3.11+ и запускаете приложение через `streamlit run app.py`, а не напрямую через `python app.py`.

### Playwright: браузер не найден

```
playwright install chromium
```

Если команда `playwright` не найдена, используйте:

```bash
python -m playwright install chromium
```

### Docker: порт занят

Если порт 8501 уже используется, измените маппинг в `docker-compose.yml`:

```yaml
ports:
  - "8502:8501"
```

и откройте `http://localhost:8502`.

### SMTP: ошибка аутентификации

Используйте **пароль приложения** (не основной пароль от аккаунта mail.ru). Создать его можно в настройках безопасности аккаунта mail.ru.

---

## Структура проекта

```
app.py                  # Streamlit UI
requirements.txt
Dockerfile
docker-compose.yml
.dockerignore
core/
  __init__.py
  settings.py           # Dataclass с параметрами поиска
  merge.py              # Объединение и дедупликация результатов
  ai_ranker.py          # AI-ранжирование (заглушка, TODO sentence-transformers)
  export_excel.py       # Экспорт DataFrame → XLSX
  email_mailru.py       # Отправка письма через SMTP mail.ru
  sources/
    __init__.py
    docsearch.py        # Заглушка для docSearch (TODO Playwright)
    orders_search.py    # Заглушка для extendedsearch (TODO Playwright)
```

---

## Дорожная карта (Roadmap)

- [x] **Playwright-скрапинг docSearch**
  - Открыть `https://zakupki.gov.ru/epz/order/docSearch/results.html`
  - Выбрать регион через модальное окно «Мой регион» → «Ваш субъект РФ» → Сохранить
  - Ввести запрос, перебрать страницы до `limit` записей
  - Вернуть DataFrame (`purchase_number`, `title`, `url`, `price`, `publish_date`, `source`)

- [x] **Playwright-скрапинг extendedsearch**
  - Аналогично для `https://zakupki.gov.ru/epz/order/extendedsearch/results.html`

- [x] **Фильтрация по дате** — передавать `date_from` / `date_to` в запросы

- [ ] **AI-ранжирование** — реализовать `score_results` в `core/ai_ranker.py`
  с использованием `sentence-transformers` (модель `paraphrase-multilingual-MiniLM-L12-v2`)

- [x] **Автоматический выбор региона** — Playwright-взаимодействие с полем «Ваш субъект РФ»

- [x] **Тесты** — покрыть `merge.py`, `export_excel.py`, `ai_ranker.py`

- [x] **Docker-поддержка** — кроссплатформенный запуск через `docker compose up --build`

- [x] **Режим mailto** — отправка e-mail без пароля через почтовый клиент

